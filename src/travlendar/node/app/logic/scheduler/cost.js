exports.eval = function evaluator(travel, arrivingTask){
    let data = require('../data_acc/dataManager');
    let manager = new data.manager();
    manager.getTravelCost(/* TODO: insert params */);
}