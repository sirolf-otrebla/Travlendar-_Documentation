let chai = require('chai');
let nativeAssert = require('assert');

describe("dayschedule", function () {
        describe("testing greedy solutions"), function () {
            it('should create a solution with all tasks', function () {
                let taskList = [
                    new FixedTask(1,"A", new Location(1,1),),
                    new FixedTask(),
                    new FixedTask(),
                    new FixedTask(),
                    new FixedTask(),
                    new FixedTask()
                ]
                //TODO
            });
            it('should create a greedy solutiom', function () {
                //todo
            });
        }

        describe('testing Dayschedule object', function () {
            it('should calculate cost correctly', function () {

            });
            it('should add tasks and travels correctly', function () {

            });
            it('should remove travels correctly', function () {

            });

        })

}
)